<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnjava">

    <application
        android:name="com.example.learnjava.Controller"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lessons.Lesson7"/>
        <activity android:name=".lessons.Lesson6" />
        <activity android:name=".lessons.Lesson5" />
        <activity android:name=".lessons.Lesson4" />
        <activity android:name=".lessons.Lesson3" />
        <activity android:name=".lessons.Lesson2" />
        <activity android:name=".lessons.Lesson1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
    </application>

</manifest>